---
- hosts: notebook
  vars_files:
  - ['secrets.yml', 'secrets.vault.yml']
  roles:
  - common
  - notebook

- hosts: proxy
  vars_files:
  - ['secrets.yml', 'secrets.vault.yml']
  roles:
  - common
  - proxy
